# Brainfuck

## A small interpreter for the Brainfuck programming language created by Urban Müller.

Notable for its extreme minimalism, the language consists of only eight simple commands, a data pointer and an instruction pointer. While it is fully Turing complete, it is not intended for practical use, but to challenge and amuse programmers. Brainfuck simply requires one to break commands into microscopic steps.  The language's name is a reference to the slang term brainfuck, which refers to things so complicated or unusual that they exceed the limits of one's understanding.

# Requirements to run
Browser(Chrome, Mozilla) || Nodejs

# Install
Clone the repo or download it into your machine from the clone button in github.

# Run on node.js
* Open It on your favorite code editor (VS CODE).
* Open the terminal on the location of the index.js file
* Run node index.js and wait to see the result

# Run on browser
* Alternatively open the index.js and copy its contents.
* Head oover to your favorite browser. Open the console tab in the developer tools. In chrome and firefox press: 
```sh
    ctr + shift + i (for windows users) || cmd + shift + i (for mac users)
```
* Paste it in your console and then wait for the result

# Test cases
* If you want to try on some new text, head to [dcode](https://www.dcode.fr/brainfuck-language) and encryot some text...get the code and create a new bf intance and call:

```sh
    Brainfuck.getOutput()
```